dependencies:
  idf: ">=5.4.0"

  # LVGL 9 — graphics library
  lvgl/lvgl:
    version: "^9.1.0"
    rules:
      - if: "target == esp32p4"

  # WiFi via ESP32-C6 coprocessor — must match C6 factory slave firmware version
  espressif/esp_hosted:
    version: "2.0.17"
  espressif/esp_wifi_remote:
    version: "^1.1"
