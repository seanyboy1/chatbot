dependencies:
  idf: ">=5.4.0"

  # LVGL 9 — graphics library
  lvgl/lvgl:
    version: "^9.1.0"
    rules:
      - if: "target == esp32p4"

  # WiFi remote — routes esp_wifi API to ESP32-C6 via SDIO (esp_hosted, slot 1)
  espressif/esp_wifi_remote:
    version: "^1.5.2"

