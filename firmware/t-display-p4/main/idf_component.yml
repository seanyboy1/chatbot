dependencies:
  idf: ">=5.4.0"

  lvgl/lvgl:
    version: "^9.1.0"
    rules:
      - if: "target == esp32p4"

  # WiFi remote — routes esp_wifi to ESP32-C6 via esp_hosted SDIO (slot 1)
  espressif/esp_wifi_remote:
    version: "^1.5.2"


